import { urlFor } from '../sanity';
const Image = ({ prodImage }) => {
  //  console.log('Image from imh', prodImage);
  return (
    <div>
      {/*<img
        src={urlFor(image).width(400).height(500).image()}
        alt="product-image"
      />*/}
    </div>
  );
};

export default Image;
